import React from 'react';
import SnapCarousel from 'react-native-snap-carousel';

class Carousel extends React.Component {
    render() {
        return <SnapCarousel data={} />
    }
};

export default Carousel;